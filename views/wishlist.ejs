<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>내 위시리스트 - My Shopping Mall</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
<header>
    <div class="logo" onclick="location.href='/'">SHOPMALL</div>
    <nav>
        <ul>
            <li><a href="/mypage">마이페이지</a></li>
            <li><a href="/user/logout">로그아웃</a></li>
        </ul>
    </nav>
</header>

<main class="container">
    <h2>내 위시리스트</h2>
    <% if (products.length === 0) { %>
        <p>위시리스트에 상품이 없습니다.</p>
    <% } else { %>
        <ul class="wishlist-list">
            <% products.forEach(product => { %>
                <li class="wishlist-item">
                    <img src="<%= product.image_url %>" alt="<%= product.name %>" />
                    <div class="info">
                        <h3><%= product.name %></h3>
                        <p><%= product.description %></p>
                        <div class="price"><%= product.price.toLocaleString() %>원</div>
                        <div class="actions">
                            <form action="/mypage/wishlist/remove" method="post" style="display:inline;">
                                <input type="hidden" name="productId" value="<%= product.id %>" />
                                <button type="submit" class="btn-remove">삭제</button>
                            </form>
                            <form action="/products/buy/<%= product.id %>" method="get" style="display:inline;">
                                <button type="submit" class="btn-buy">구매하기</button>
                            </form>
                        </div>
                    </div>
                </li>
            <% }) %>
        </ul>
    <% } %>
</main>

<footer>
    &copy; 2025 My Shopping Mall
</footer>
</body>
</html>
