<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>공지사항 - My Shopping Mall</title>
    <link rel="stylesheet" href="/stylesheets/notice.css" />
</head>
<body>
<%- include('header') %>
<main class="container">
    <h2>공지사항</h2>
    <ul class="notice-list">
        <% notices.forEach(notice => { %>
            <li>
                <strong><%= notice.title %></strong> <span class="date"><%= new Date(notice.created_at).toLocaleDateString() %></span>
                <p><%= notice.content %></p>
            </li>
        <% }) %>
    </ul>
</main>
<%- include('footer') %>
</body>
</html>
