<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title><%= post ? '글 수정' : (parentId ? '답글 작성' : '글쓰기') %></title>
    <link rel="stylesheet" href="/stylesheets/board.css" />
</head>
<body>
<h2><%= post ? '글 수정' : (parentId ? '답글 작성' : '글쓰기') %></h2>

<% if (parentTitle) { %>
    <p>📝 원글 제목: <strong><%= parentTitle %></strong></p>
<% } %>

<form method="POST" action="<%= post ? '/board/edit/' + post.id : (parentId ? '/board/reply/' + parentId : '/board/new') %>">

    <!-- 작성자 입력란 제거됨 -->

    <label for="title">제목</label><br />
    <input type="text" id="title" name="title" value="<%= post?.title || '' %>" required /><br /><br />

    <label for="content">내용</label><br />
    <textarea id="content" name="content" rows="8" required><%= post?.content || '' %></textarea><br /><br />

    <button type="submit"><%= post ? '수정하기' : '등록하기' %></button>
</form>
</body>
</html>
